import React from "react";

const HelpButton = ({initials}) => {
    return (
        <div className="relative">
            <span className="bg-white rounded-full p-2 text-gray-500 text-bold h-[20px] w-[20px] flex items-center justify-center" > ? </span>
        </div>
    );
};

export default HelpButton;



